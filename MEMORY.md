# MEMORY.md - Lưu trữ kiến thức và tiến trình

## Tài nguyên API
- **Gemini:** Đã nạp 11 key vào `apis.txt`.
- **Trạng thái:** Đã check qua `token_monitor.js`, tất cả đều đang hoạt động tốt (không có lỗi 429 hay 401).

## Các nguyên tắc hành động (Hard Rules)
- **Restart Gateway:** KHÔNG ĐƯỢC TỰ Ý restart gateway (`openclaw gateway restart`) nếu không có lệnh trực tiếp từ Mộc Nguyễn. Phải xin phép hoặc đợi lệnh.
